// DS rules for CSS, the half ESLint cannot reach. At error from the start:
// a warning nobody reads is a stale list with another name.
import { configs } from "@adamarant/designsystem/stylelint"

export default configs.recommended
